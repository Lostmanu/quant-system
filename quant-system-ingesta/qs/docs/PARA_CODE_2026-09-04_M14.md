# Relevo a Code — M-14 WIP (2026-09-04)

> **HISTÓRICO SUPERADO TRAS LA AUDITORÍA DE CODE.** Este era el relevo anterior al dictamen
> `PARA_LA_MESA_2026-09-04_AUDITORIA_M14.md`. El estado operativo vigente está en
> `PARA_CODE_2026-09-04_M14_REPARACION.md`; no use los conteos ni el contrato de esquema de este
> documento como descripción del árbol actual.

Este documento registra el corte exacto después de la implementación de Codex. No
ratifica el lote. Code debe revisarlo de forma independiente y ejecutar las pruebas
largas; no debe interpretar este relato como evidencia sustitutiva del diff.

## Límites vigentes

- Rama: `codex/wip-m12.20`.
- Base/HEAD sin reescribir: `58ea72e82c457aa393d868990beec679837a4a8c`.
- Cambios WIP sin commit y sin push.
- No ejecutar sonda, no gastar créditos, no crear freezes y no leer LIT/DOGE
  protegidos ni abrir sus `.npz`.
- STOP de producción vigente.
- Los untracked preexistentes (informe, anatomía y recibos antiguos) son ajenos a
  este lote y no se deben borrar, mover, añadir ni modificar.

## Alcance modificado por Codex

Hay 16 ficheros tracked modificados (`+1486/-231` en el último corte):

- `.gitignore`.
- `analysis/eco_cas.py`, `analysis/eco_gate_b.py`.
- `tools/censo_anclas.py`, `tools/estructura_chd_local.py`,
  `tools/mutacion_gate_b.py`, `tools/mutacion_ref_valida.py`, `tools/prevuelo.py`.
- `tests/conftest.py`, `tests/test_censo_anclas.py`,
  `tests/test_cruce_ancla_por_ancla.py`, `tests/test_eco_cas.py`,
  `tests/test_eco_gate_b.py`, `tests/test_estructura_chd.py`,
  `tests/test_gate_dos_worktrees.py`, `tests/test_mutacion_gate_b.py`.

## Qué se cambió

1. Gate: ruta canónica compartida entre worktrees; lock y sellado bajo autoridad
   común; autenticación exacta del lock contra `sha256_lock`; reautenticación justo
   antes del gasto; contención recursiva de tests; carrera real de dos worktrees.
2. CAS/máquina: capacidad de durabilidad derivada localmente; alta no-clobber;
   prefijos de crash tipados; replays exactos con preestado y postestado sellados;
   deserialización estricta y autoridad nunca aportada por el llamador.
3. Censo: recibo esquema 3 estricto y autosellado; pre-vuelo antes de leer parquet;
   post-vuelo; mapa SHA completo; conciliación exacta de destinos; ligadura explícita
   `bytes capturados al importar == bytes actuales en disco == blob de HEAD`, tanto
   para el censo como para la máquina importada.
4. Arnés: asociaciones antiguas reancladas como mutaciones de propiedad completas.
   No se aceptaron excepciones colaterales como mordida ni se relajaron expectativas.

## Evidencia local ya obtenida

- Focales CAS/máquina/censo/cruce: `182 passed`.
- Arnés `mutacion_ref_valida.py`: `190/190` muerden; línea base
  `290 passed, 368 deselected`; los 212 tests declarados existen; `rc=0`.
- Arnés `mutacion_gate_b.py`: `14/14`; línea base `18/18`; árbol vigilado intacto.
- Pre-vuelo: gate 14, libro 187, referencia 190; sin anclas muertas ni tests
  inexistentes.
- `git diff --check`: sin errores (solo avisos locales LF/CRLF).

La suite completa se interrumpió por apagado/parada de la aplicación alrededor del
55 %. No mostró `F` ni `E` hasta ese punto, pero eso **NO es evidencia** y no tiene
veredicto. No se debe citar como pasada ni como no-regresión.

## Trabajo de Code — en este orden

1. Auditar el diff sin confiar en este resumen. Buscar P0/P1 propios antes de cambiar.
2. Ejecutar desde `quant-system-ingesta/qs`:
   - `.venv\\Scripts\\python.exe tools\\prevuelo.py --permitir-sucio`
   - `.venv\\Scripts\\python.exe -m pytest tests/ -q`
   - `.venv\\Scripts\\python.exe tools\\mutacion_libro_tx.py`
   - `.venv\\Scripts\\python.exe tools\\mutacion_gate_b.py`
   - `.venv\\Scripts\\python.exe tools\\mutacion_ref_valida.py`
   - `git diff --check` y `git status --short`
3. Si algo falla, atribuir la causa antes de editar. No aflojar tests, mensajes ni
   mutantes para conseguir verde. Registrar cada cambio adicional por fichero y razón.
4. No hacer push ni lanzar CI sin autorización expresa de Manuel. Entregar primero el
   resultado local y la auditoría del diff.

## Lentes adversariales obligatorias

- `eco_cas.publicar_una_vez`: prefijos de crash, `link` instalado seguido de fallo de
  fsync, retirada autenticada del temporal, retry y verdad del límite Windows.
- CAS: replay exacto, autenticidad del postestado, token completo, contención de ruta
  y ausencia de autoridad aportada por el llamador.
- Censo: esquema/totales exactos, identidad ejecutado-disco-HEAD en pre y post-vuelo,
  y garantía de que no se leen datos antes del pre-vuelo.
- Gate: misma autoridad y ruta real entre worktrees, reautenticación pegada al gasto
  y contención que impida sellar el árbol real desde tests.
- Mutaciones agrupadas: comprobar que cada grupo representa una sola propiedad y que
  el fallo viene de la aserción declarada, no de sintaxis, colección o excepción
  incidental.

## Formato de vuelta

Code debe devolver: SHA/base, `git status`, diffstat, hallazgos ordenados P0/P1/P2,
qué modificó exactamente, resultados completos con `rc`, y una lista explícita de lo
no acreditado. Nada se presenta como ratificado: la mesa vuelve a auditar el resultado.
